<div class="row">
  <div class="col-md-6 offset-md-3">
    <h1>
      <%= @user.username %>
    </h1>

    <%= render "users/user_tabs"%>

  </div>
</div>



  <div class="row mb-4">
    <div class="col-md-6 offset-md-3">
      <div class="list-group">
        <% @user.followers.each do |follower| %>
          <%= link_to "#{follower.username}", "/#{follower.username}", class: "list-group-item list-group-item-action"%>
        <% end %>
      </div>
    </div>
  </div>
